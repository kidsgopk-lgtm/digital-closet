import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "next-themes";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Digital Closet - Shop Your Closet",
  description: "Your personal digital wardrobe manager. Generate outfits, track what you own, and never ask 'what should I wear' again.",
  keywords: ["Digital Closet", "Outfit Generator", "Wardrobe Manager", "Fashion", "Outfit Planning"],
  authors: [{ name: "Digital Closet Team" }],
  openGraph: {
    title: "Digital Closet - Shop Your Closet",
    description: "Your personal digital wardrobe manager. Generate outfits and shop your closet.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
